JDK 历史：
    jdk1.5是jdk对自己一次非常重要的扩充：
    jdk1.5 加入了自动装箱，泛型，动态注解，枚举，可变长参数，遍历循环等语法特性。

    java程序运行在64位虚拟机上需要付出比较大的代价，
        内存问题：由于指针膨胀和各种数据类型对其补白的原因，运行在64位系统上的java应用需要消耗更多的内存，通常要比32位系统额外增加10%-30%的内存消耗。
        其次 64位虚拟机的运行速度在各个测试项上几乎都全面落后于32位虚拟机，两者差不多有15%左右的性能差距。
    在jdk1.6之后，jdk提供了普通对象指针压缩功能，在解释器解释字节码时，织入压缩指定以节省内存消耗。




